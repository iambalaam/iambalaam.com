import * as React from 'react';

import * as s from './Header.module.scss';

export default function Header() {
    return (
        <header className={s.header}>
            Guy Balaam - Software Developer
        </header>
    );
}